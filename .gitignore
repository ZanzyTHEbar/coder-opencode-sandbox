# Terraform (keep .terraform.lock.hcl for reproducible builds)
template/.terraform/
template-kubernetes/.terraform/
infra/terraform/**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# Local env (do not commit secrets)
coder-deployment/.env
